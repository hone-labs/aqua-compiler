let a;
a = 3;
const x = 5;
const y = 8;

function add(a, b): uint64 {
    return a + b;
}

function multiply(a, b): uint64 {
    return a * b;
}

if (x + y < 5) {
    return add(x + y);
}
else {
    return multiply(x, y);
}

